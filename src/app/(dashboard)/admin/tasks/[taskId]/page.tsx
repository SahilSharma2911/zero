import React from 'react'


type paramsProps ={
    params?: Promise<{ taskId: string }>
}


const page = ({params}: paramsProps) => {
  return (
    <div>
      
    </div>
  )
}

export default page
